{% extends 'base.html.twig' %}

{% block body -%}
    <h1>Creature creation</h1>

    {{ form_start(form, {'method': 'POST', 'action': 'create'}) }}
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
    {{ form_end(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('creature_index') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
